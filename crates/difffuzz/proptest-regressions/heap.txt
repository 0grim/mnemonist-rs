# Seeds for failure cases proptest has generated in the past. It is
# automatically read and these particular cases re-run before any
# novel cases are generated.
#
# It is recommended to check this file in to source control so that
# everyone who runs the test benefits from these saved cases.
#
# PROVENANCE OF THE ENTRY BELOW -- read before assuming it recorded a real port
# defect. It did not. It was produced by a deliberate falsification of the
# fuzzer, in the spirit of DoD gate 6 and D-32: a fuzzer that has never been
# seen to fail is just a second green light.
#
# The sabotage was to make `Heap::clear` truncate the backing array in place
# instead of REBINDING it -- i.e. to collapse D-41, which is the single most
# plausible way a future cleanup breaks this port, since `set_length(0)` and
# `allocate(0)` leave an *identical* observable state for every program whose
# comparator has no side effects.
#
# That is exactly why this seed matters. Under the sabotage:
#
#   * all 14 assertions of test/heap.js still passed;
#   * all 7 of test/fixed-reverse-heap.js still passed;
#   * the fuzzer found it in 0.1s and shrank it to three operations.
#
# The three operations are only a divergence because the comparator MUTATES:
#
#     var s = new Heap(clearer);   // clears the heap on its first comparison
#     s.push(1);
#     s.pushpop(18);               // upstream 1, truncating port undefined
#
# `clearer` calls `heap.clear()` from inside the sift. Upstream's clear installs
# a NEW array, so the sift finishes into the detached one and the live array
# stays empty; a truncating clear leaves the sift writing back into the live
# array. A grammar whose comparators were all pure -- which is every comparator
# in the original suite -- could not have produced this.
#
# The sabotage was reverted and this seed now passes. It stays checked in as a
# regression guard, and proptest replays it before any novel case.
cc e2f75b3f72f2f38cff072db519872195255efc87567df8e626fc83b9c31f66e7 # shrinks to Program { ctor: [Object {"$factory": String("clearer")}], ops: [Op { name: "push", args: [Number(1)] }, Op { name: "pushpop", args: [Number(18)] }] }
