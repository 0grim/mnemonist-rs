# Declared port scope: one module name per line (matches tests/original/test/<name>.js).
# A module is listed ONLY when it is complete: port + original test green + fuzz + bench.
# Consumers: tests/run.sh spec selection, README scope table, .port-mortem.toml.

static-disjoint-set
bit-set
heap
lru-cache
trie
vector
# Appended for the sequence-backed Gate 10 batch (2026-08-02): all eleven were
# already shim/test/doc/fuzz-complete per scripts/status.sh and needed only
# the benchmark. See bench/results.json and each docs/modules/<unit>.md's own
# "### Bench" section for figures.
stack
queue
fixed-stack
fixed-deque
circular-buffer
hashed-array-tree
sparse-map
sparse-queue-set
bit-vector
suffix-array
sort
# sparse-set  -- DESCOPED 2026-08-01: forEach with a mutating callback diverges
#                (upstream [1,2], port [1,2,3,4]). See B-31. Re-add once fixed and fuzzed.
#
#                STILL DESCOPED, and deliberately so. B-31 is FIXED (the bridge holds
#                RefCell<Core>; the repro prints MATCH) and re-fuzzed with the new
#                $forEach op, two seeds, zero divergences. What is missing is gate 10:
#                the RefCell adds a borrow flag check to every access and nobody has
#                measured it. Benchmarks need an idle machine (DESIGN.md 7.3), so the
#                figure has to come from the quiet serial pass, not from a run taken
#                while agents are working. Re-add after that pass, not before.
#
#                2026-08-02: the quiet pass happened (bench/runner/src/sparse_set.rs
#                ::run_mixed_refcell, `--refcell-probe`). The isolated borrow-flag cost
#                is NOT distinguishable from run-to-run noise at this workload's size:
#                three repeated probes at size 1e6 gave RefCell p50 anywhere from 3%
#                below to 2% above the plain figure, and a measured=30 run had the
#                RefCell variant faster. This is a real measurement, not an absence of
#                one -- but it answers "is the borrow check the dominant bridge cost?"
#                with "not detectably, on its own, at this size", not with a number to
#                publish as a regression. Still descoped: that call is the orchestrator's,
#                not this agent's, and the probe is deliberately not gate-10 evidence (no
#                upstream JS analogue exists to compare a bare RefCell against).
#                2026-08-02 (orchestrator): RE-SCOPED. The condition this block set
#                -- "re-add after that pass, not before" -- is met, and met with a
#                measurement rather than an absence of one. B-31 is fixed, re-fuzzed
#                with the $forEach op at zero divergences, and the isolated borrow-flag
#                cost is below this host's run-to-run noise. A cost that cannot be
#                distinguished from noise is not a cost we can honestly report as a
#                regression, and gate 10's `regressions` array says so explicitly.
sparse-set
# Appended for the map-like/multi-container Gate 10 batch (2026-08-02): all
# nine were already shim/test/doc/fuzz-complete per scripts/status.sh and
# needed only the benchmark. See bench/results.json and each
# docs/modules/<unit>.md's own "### Bench" section for figures. Two lost on
# p50/p99 (default-map, bi-map) and two more lost on a single metric
# (multi-set's min_ns_per_op, multi-array's p50_ns_per_op) -- stated plainly
# rather than smoothed into a clean sweep; default-map's loss was re-checked
# at 4x domain (mixed-4e6) and holds at both sizes, so it is a real,
# reproducible cost rather than noise.
#
# `default-weak-map` is deliberately NOT in this batch: its keys must be
# objects and entries vanish at the garbage collector's discretion, so timing
# would be dominated by allocation and GC rather than by the structure --
# see docs/modules/default-weak-map.md's own "### Bench" section for the
# reasoning. A stated exclusion is the honest call here, not a number nobody
# could trust.
default-map
bi-map
multi-map
multi-set
multi-array
fuzzy-map
fuzzy-multi-map
inverted-index
set
