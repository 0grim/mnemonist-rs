// Shim standing in for upstream `fixed-critbit-tree-map.js`.
//
// See `tests/bridge/critbit-tree-map.js` — same mechanism, different class.
module.exports = require('@port/addon').FixedCritBitTreeMap;
