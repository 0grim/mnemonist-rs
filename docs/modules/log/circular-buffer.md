# circular-buffer — working log

Chronological. See `docs/modules/circular-buffer.md` for the current-state document and
`docs/modules/evidence/circular-buffer.md` for the gate artifacts.

## Scope status corrected

The document previously stated "Neither is in `tests/scope.txt` yet — gate 10 is outstanding for
both," referring to `fixed-deque` and `circular-buffer`. Both units have since completed gate 10 and
are listed in `tests/scope.txt`. The current document no longer carries that now-stale claim.
