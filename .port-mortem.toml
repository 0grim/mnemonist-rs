# Port Mortem 2026 — submission metadata
# No schema was published. The three fields named in the brief (track, source, kickoff hash)
# are top-level; everything else is namespaced.

track        = "G"     # JavaScript -> Rust per the admin FAQ. Confirm before submission.
source       = "https://github.com/Yomguithereal/mnemonist"
# sha256 of tests/SHA256SUMS, which itself covers every vendored upstream test file.
kickoff_hash = "sha256:5e24c557192245b594ca028844a3f685f2e91d9c1daf7f5e88a08576a778e674"

[upstream]
commit          = "1f2c752049eb3439f3987310391ae5e123e97dfe"
commit_date     = "2026-04-30"
version         = "0.40.4"
license         = "MIT"
source_language = "JavaScript"
target_language = "Rust"
loc_total       = 15386
modules_total   = 44
tests_total     = 525          # measured pre-kickoff, Node 24.18.1, clean clone

[port]
scope_manifest = "tests/scope.txt"
modules_ported = 0
loc_in_scope   = 0

[verify]
build           = "docker build -t port-mortem ."
test            = "docker run --rm port-mortem"
test_repo_wide  = "docker run --rm port-mortem ./tests/run.sh all"
core_standalone = "docker build -t pm-core --target core . && docker run --rm pm-core"
hashes          = "sha256sum -c tests/SHA256SUMS"

[results]
in_scope_passing  = 0
in_scope_total    = 0
repo_wide_passing = 0
repo_wide_total   = 525
